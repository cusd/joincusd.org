<% content_for(:title, project.name) %>

<section class="project-description">
  <div class="project-description__logo">
    <div class="medium-up icon icon--project-<%= short_name %> icon--project-<%= short_name %>--colored"></div>
    <div class="small-only icon icon--project-<%= short_name %> icon--project-<%= short_name %>--colored-bg"></div>
  </div>
  <div class="project-description__main">
    <h1><%= project.name %></h1>
    <h2><%= project.tagline %></h2>
    <div class="project-description__text">
      <input type="checkbox" class="see-more__checkbox" id="seeMore">
      <%= project.description %>
      <label for="seeMore" class="see-more__label">Read <span class="see-more__label--closed">More</span><span  class="see-more__label--open">Less</span></label>
    </div>
    <ul class="project-description__gallery">
      <% project.photos.each do |p| %>
      <li>
        <%= image_tag p %>
      </li>
    <% end %>
    </ul>
  </div>

</section>

<section class="project-openings">
  <div class="project-openings__wrapper">
    <h3>What We're Looking For</h3>
    <ul class="project-openings__list" data-equalizer data-equalizer-mq="medium-up">
      <% if project.openings %>
        <% project.openings.each do |o| %>
          <li>
            <div class="panel" data-equalizer-watch>
              <h4><%= o.title %></h4>
              <p><%= o.desc %></p>
              <%= link_to "Apply Now", o.link || 'https://docs.google.com/a/cornell.edu/forms/d/1xoWpz59v7SkpzRUcXRuvBT0qw4RmWI0didgraXt-Vj0/viewform', class: 'button' %>
            </div>
          </li>
        <% end %>
        <li>
          <div class="panel" data-equalizer-watch>
            <h4>Missing Something?</h4>
            <p>Don’t see that perfect fit? No worries, apply as a wildcard!</p>
            <%= link_to "Apply Now", 'https://docs.google.com/a/cornell.edu/forms/d/1xoWpz59v7SkpzRUcXRuvBT0qw4RmWI0didgraXt-Vj0/viewform', class: 'button' %>
          </div>
        </li>
      <% else %>
        <li class="project-openings__no-openings">
          <div class="panel">
            <h4>Nobody :'(</h4>
            <p>We're not actively recruiting for this project right now. Feel free to <a href="mailto:cu.sustainabledesign@gmail.com">email us</a> and tell us why we should be though.</p>
          </div>
        </li>
      <% end %>
    </ul>
  </div>
</section>